#!/bin/zsh

# -----------------------------------------------
#   Author: Liam Howell
#   Description: No magic here, just load `.zshrc`
# -----------------------------------------------

[ -n "$PROMPT" ] && source "$HOME/.zshrc"
